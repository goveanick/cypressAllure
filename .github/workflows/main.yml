name: E2E on Chrome

on: 
  workflow_dispatch:

jobs:
  install:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Install Cypress
        run: yarn

      - name: Cypress run
        continue-on-error: true
        run: yarn cypress run --env allure=true
